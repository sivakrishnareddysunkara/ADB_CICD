# Azure Pipelines is DISABLED for this repository.
# CI is handled entirely by GitHub Actions (.github/workflows/cicd.yml).
# Triggers are set to none to prevent "no hosted parallelism" / missing agent errors.

trigger: none
pr: none

pool:
  vmImage: ubuntu-latest

steps:
- script: echo Azure Pipelines is disabled. See GitHub Actions for CI.
  displayName: 'CI disabled - use GitHub Actions'
