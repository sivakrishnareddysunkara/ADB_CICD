# NOTE: This pipeline has been migrated to GitHub Actions (.github/workflows/cicd.yml).
# GitHub Actions provides free CI/CD minutes and does not require a hosted parallelism grant.
# See: https://github.com/sivakrishnareddysunkara/ADB_CICD/actions

trigger:
- main

pool:
  name: Default

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- script: |
    echo Add other tasks to build, test, and deploy your project.
    echo See https://aka.ms/yaml
  displayName: 'Run a multi-line script'
